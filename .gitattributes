# ─────────────────────────────────────────────────────────────
# 배포 저장소는 바이너리 산출물 저장소다.
#
# ★ 줄바꿈 변환을 절대 허용하지 않는다 ★
#   코드 서명 정보인 _CodeSignature/CodeResources 는 XML plist 라서
#   git 이 텍스트 파일로 오인할 수 있다. core.autocrlf 가 켜진 PC 에서
#   체크아웃하면 CRLF 변환이 일어나 서명이 무효가 된다.
#   (개발자마다 git 설정이 다를 수 있으므로 저장소 차원에서 고정한다)
# ─────────────────────────────────────────────────────────────

* -text

*.xcframework/** binary -diff -merge
